package example

import cats.effect.IO
import org.http4s._
import org.http4s.dsl.io._

object SimpleReproduction {

  val pathologicalHttp4sDslRouteDefinition: HttpRoutes[IO] = HttpRoutes.of {
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
    case GET -> Root / "blah" / LongVar(s) => Ok()
    case DELETE -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" => Ok()
    case PUT -> Root / "blah" / LongVar(s) => Ok()
    case POST -> Root / "blah" / LongVar(s) / "a" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "b" / "c" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "d" / "e" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "f" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "g" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "h" / "i" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "j" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "j" => Ok()
    case POST -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "k" / "i" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "grant" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "l" / "remove" => Ok()
    case PUT -> Root / "blah" / LongVar(s) / "m" => Ok()
  }

}
